<div class="container">
  <div class="row justify-content-between">
    <article class="col-lg-40 order-lg-1 section-push">
      <div class="rte">
        <h2>{{ page.title }}</h2>
        {{ page.content }}
      </div>
      {% section 'page-template--size-chart' %}
    </article>
    {% section 'page-sidebar' %}
  </div>
</div>
